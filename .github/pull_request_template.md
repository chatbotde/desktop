## Summary

<!-- What and why. -->

## Test plan

- [ ] `cd frontend && npm test` (if UI changed)
- [ ] `cd frontend && npm run lint`
- [ ] Manual: `npm run dev` and exercise the changed path

## Notes

- Protocol changes in `remote-pad/` need a matching PR in [remote-desktop](https://github.com/sonicthinking/remote-desktop).
- Do not include `.env`, keys, or keystores.
