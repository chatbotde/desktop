{
  "name": "interface-window",
  "version": "1.0.0",
  "description": "Interface window module for SonicThinking - TypeScript setup",
  "main": "interface-window.js",
  "scripts": {
    "build": "npx tsc",
    "build:dev": "npx tsc -p tsconfig.dev.json",
    "dev": "npx tsc --watch -p tsconfig.dev.json",
    "dev:watch": "npx tsc --watch",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "@types/node": "^20.0.0"
  },
  "dependencies": {}
}