/**
 * System Prompts
 *
 * Prompt text is centralized under `src/lib/prompt/prompts/` so it can be reused
 * across the app. This file exists to preserve the original `@/lib/ai` exports.
 */

export * from '@/services/prompts/prompts/system-prompts';
