/**
 * Central export point for registry
 */

export {
  ProviderRegistry,
  providerRegistry,
  getCurrentProvider,
  setCurrentProvider,
  getProvider,
  getAllAvailableModels,
  getProviderStatus,
  type ProviderName,
} from './provider-registry';
