/**
 * AI Services
 * 
 * AI providers, model config, and unified service
 * 
 * @example
 * import { UnifiedAIService, getModelConfig } from '@/services/ai'
 */

// Re-export from current location (will migrate files later)
export * from '@/lib/ai'
